# Scrapy's project marker. `scrapy crawl` walks up from the working directory
# looking for this file to discover which settings module to use.
#
# Deliberately created in Step 4 rather than Step 1: until the settings module
# it points at actually existed, this file was a pointer to nothing.

[settings]
default = wrc_pipeline.scraper.settings
